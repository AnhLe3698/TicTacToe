Our Project will cover the following concepts

Classes
  -Inheritance
  -Compisition
  -Getters/Setters
Objects
  -looping through Objects
  -objects methods and properties (ex: Object.keys() Object.values(), Object.keys().length)
  -"this" reference
  -recursion
  -For.. in
Strings
  -Looping through strings
  -Type Coersion
  - For..of
  -Typeof detection of numbers
  -string methods like .length
Numbers
  -Detecting Integers
  -detecting if a string is numbers
  -rounding using math.floor and math.round
  -Math class method library ex. Math.sqrt() Math.pow(num, exp), Math.random()
  - Type coersion
Arrays
  - Array methods, .map(), .length, .join(), .slice, .splice add more
  - For..of
  - recursion looping
  -

Styling
- Commenting
- consistancy 
- Eslint

General concepts
- null vs undefined and where they show up and are used
- primitive data types Boolean, Undefined, String, Numbers, Symbol, Null, Big Numbers
- command line inputs process.argv.slice(2)
- exporting and importing modules modules.export = {}, let importedModule = requre('./folder/filename')

Callbacks -
- Passing functions as a parameter
- Higher order functions
- Using callbacks to handle asyncronous blocks of sequentiontial code
- Using callbacks as a form of Inheritance and Compisition

Unit Testing - 
- Mocha framework
- Chai assertions library
- TDD Test driven developement

Recursion -
- Functions calling itself
- Considering the base case 
- Determining nested arrays
- Determining Nested objects
- Sorting functions

Functions as an objects
- function methods and property assignments
- Anonymous functions
- Arrow functions

ASYNC funcitons:
-read/write files
-setTimeout
-setInterval
-request library/ promises library
-async using callbacks
-async using Promises classes
-Testing async functions
-readline, stdin.on
-stdout.out.write

Net library functions:
- create client
- create server
- making requests and promises style requests
- API requests

Http Library functions-
-Http protocols, createServer
-Http protocols for making client requests

Express library EJS
- Server-side rendering of HTML templates
- Process - require librarys, setup server object, setup middleware, setup listener, then manage all the paths!
- Javascript integrated logic into HTML ejs file
- HTTP style requests using GET and POST, standard HTTP status codes (ex 302, 404, 400 etc)
- Delete and Update will need some extra libraries

Bootstrap Library
- Forms for buttons
- Forms for inputs
- Multiple classes for all the different labels availible to HTML

JQuery Library
- asyncronous handling

HTML
-buttons, inputs, hidden inputs, paragraphs, tags, styles and values

CSS

Git Version Control
- Branches, merging branches by feature
- Good commiting practices, comments and frequency.


